<%= form_for @voter, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :session_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :session_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :logon_time, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :logon_time, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                voters_path, :class => 'btn' %>
  </div>
<% end %>
